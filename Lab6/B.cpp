#include <iostream>
#include <fstream>
#include <vector>

using namespace std;

int main() {
    ifstream inputf("IOfiles/check.in");
    ofstream outputf("IOfiles/check.out");
    int n;
    vector<int, pair<int, int>> nodes(n);
    for (int i = 0; i < n; i++) {

    }
}